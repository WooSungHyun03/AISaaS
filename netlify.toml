[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  # Long enough for AI generation calls made from Route Handlers / Server Actions.
  node_bundler = "esbuild"

[dev]
  command = "npm run dev"
  targetPort = 3000
